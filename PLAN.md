# Samsung Academy LXP — 2인 협업 개발 계획

> 작성일: 2026-07-27 · 근거 자료: (양식3) K-디지털 트레이닝 학습데이터관리시스템 내역서.hwp, 메뉴구성도_IA(김민아).xlsx, 현재 코드베이스 분석

---

## 1. 현재 상태 요약

### 완료된 것 (프론트엔드)
- 정적 HTML **155개 페이지** 완성: 로그인/가입(5), 관리자(79), 강사(48), 훈련생(28)
- CSS 46개 + JS 45개 (더미 데이터 렌더링, 비디오 플레이어, PDF.js 문서 뷰어 + 진도 추적, 챗봇 UI 등)
- IA 설계 완료: 메뉴 구성, 권한 정의(CRUD 매트릭스), URL/컨트롤러 패키지 설계(`com.ssa.lms.web.*`)까지 엑셀에 확정

### 없는 것 (전부 백엔드)
- 서버 코드 0줄. DB 없음. 인증 없음. API 없음.
- JS 곳곳에 "Thymeleaf `th:each` 교체 대비" 주석 → **Spring Boot + Thymeleaf**를 전제로 설계되어 있음
- fetch 호출은 4~5곳에 스텁으로만 존재 (`/api/courses/{id}`, `/api/notices/{id}` 등)

### 결론
**남은 작업의 90%는 백엔드 구축 + 프론트 연동(정적 HTML → Thymeleaf 템플릿 전환)이다.**
따라서 역할 분담은 "관리자 화면 vs 강사 화면"이 아니라 **백엔드 도메인 단위 세로 분할**(각자 DB→서비스→컨트롤러→화면 연동까지 끝까지 담당)이 맞다. 이렇게 해야 두 사람이 Claude Code로 병렬 작업할 때 파일 충돌이 거의 없다.

---

## 2. 내역서(정부 제출 양식) 필수 요건 ↔ 구현 매핑

| 내역서 요건 | 해당 기능 | 현재 상태 |
|---|---|---|
| 훈련생 유의사항 등재 (정보제공) | 훈련생 초기화면/공지 | 화면만 있음 |
| 수강신청 현황 웹 조회 (성명·과정명·개시/종료일·수강일) | 나의 과정, 수강신청 | 화면만 있음 |
| **본인인증** + 개인정보 수집·제3자 제공 동의 | 로그인/가입 + **평가 응시 시 재인증** | 화면만 있음 (동의 절차·인증 로직 필요) |
| 시험·과제·평가결과 웹 조회 | 온라인 테스트, 과제, 성적 | 화면만 있음 |
| 평가유형별(진행단계/최종/과제) 본인인증 | 시험 입장 시 인증 단계 | **화면도 없음 — 신규 필요** |
| 수료기준의 적정성 | 이수 기준 설정·자동 이수 처리 | 화면만 있음 |
| 교강사: 진도 관리, 시험·과제 채점 | 강사 모듈 전체 | 화면만 있음 |
| 관리자: 과정개설·수료기준·훈련생·교강사 관리 | 관리자 모듈 전체 | 화면만 있음 |
| 훈련별 데이터 **독립적 수집** | DB 설계 시 과정(course) 단위 데이터 분리 | 설계 필요 |
| 훈련 정보 **최소 3년 보존** | 삭제 정책(soft delete), 백업 정책 문서 | 설계 필요 |
| SSL/암호화, 보안조치 증빙 | HTTPS, 비밀번호 해시, 개인정보 DB 암호화, 접속 로그 | 구현+문서 필요 |
| 고객 의견 수렴·불만 처리 | Q&A/튜터링/설문 | 화면만 있음 |

> ⚠️ 내역서는 기능뿐 아니라 **증빙 문서**(서버 사양, 백업 정책, 보안 확인서, DB 암호화 스크린샷, DNS, 네트워크 구성도)도 요구한다. 개발과 병행해서 스크린샷·문서를 그때그때 수집할 것. 상세 항목은 아래 §2-1 체크리스트 참고.

---

## 2-1. 내역서 [붙임] 전산 인프라·보안 증빙 체크리스트

내역서 붙임("K-디지털 트레이닝 LMS 등 훈련인프라 내역")이 요구하는 항목별로, 우리가 구현·준비할 것과 제출할 증빙물을 매핑한 것.

| 붙임 항목 | 구현/준비할 것 | 제출 증빙물 | 담당 |
|---|---|---|---|
| **서버 사양** (Web/동영상/DB/백업 서버별 대수·CPU·Memory·HDD·RAID) | 배포 환경 확정 — 클라우드(AWS/NCP 등) 또는 IDC. 클라우드면 인스턴스 사양표로 대체 | 서버 사양표, 네트워크 구성도, 서버(콘솔) 사진 — IP 식별 가능해야 함 | 공동(계약은 기관) |
| **전산 장비** | IDC/호스팅/장비 임대·네트워크 계약 | 계약서 사본 | 기관/공동 |
| **백업 정책** | ① DB 자동 백업 스크립트(mysqldump 또는 클라우드 스냅샷, 일 단위) ② 웹로그 보관 ③ 백업 일지·대장 양식 ④ 장애 대응 프로세스 도표 | 백업 스케줄 문서, 백업 일지, 웹로그 백업 자료, 장애대응 플로차트 | **A** |
| **보안 관련** | ① HTTPS/TLS 적용(보안서버) ② 서버 백신·보안 SW ③ 방화벽/보안그룹 설정(클라우드면 Security Group + WAF로 IPS·웹방화벽 갈음) | 보안서버(SSL) 설치 확인서, 보안 SW 확인서, IPS/웹방화벽 설치 확인서(또는 클라우드 보안 설정 스크린샷) | **A** |
| **DB 서버 (HA)** | DB 이중화 — 최소 Primary-Replica 복제 구성(MySQL replication 또는 클라우드 Multi-AZ) | HA 확인서, DB 구축 확인서, HA 구성도 이미지 | **A** |
| **DBMS** | OS·DBMS 라이선스 정리(MySQL Community/MariaDB는 라이선스 문서로 명시) | OS 및 DBMS 라이선스 증빙 | 공동 |
| **DNS 환경** | 도메인 확보 + DNS 설정 | DNS 설정 스크린샷, IP 주소 | 공동 |
| **UPS** | IDC/클라우드 이용 시 제외 가능 → 제외 사유 명기 | (해당 시) 제원 자료 | — |
| **암호화** | ① 비밀번호: bcrypt 해시(Spring Security 기본) ② 개인정보 컬럼(전화번호·이메일·생년월일 등): AES-256 암호화 — JPA `AttributeConverter`로 구현 ③ 전송구간: TLS | **DB 암호화 스크린샷**(DB 조회 시 암호문으로 보이는 화면 + 적용 알고리즘 명세) | **A** |
| **LMS 플랫폼 URL 표** | 훈련생/훈련교사/관리자 모듈별 접속 URL | 모듈별 URL 표 (배포 후 작성) | 공동 |

**개발 단계에서 미리 해두면 증빙이 쉬워지는 것들:**
- 개인정보 컬럼 암호화는 **Phase 0 엔티티 설계 때부터** `AttributeConverter`를 붙여야 한다. 나중에 붙이면 기존 데이터 마이그레이션이 필요해짐.
- 백업 스크립트·접속 로그(access_log 테이블)도 개발 초기부터 넣어두고, 운영 데이터가 쌓인 시점에 스크린샷을 찍는 것이 증빙 품질이 좋다.
- 서버 계약(IDC vs 클라우드)은 개발과 무관하게 **기관 의사결정이 필요한 항목**이므로 조기에 확인할 것.

---

## 3. 역할 분담

### 공통 원칙
- 각자 자기 도메인의 **엔티티 → 리포지토리 → 서비스 → 컨트롤러 → Thymeleaf 연동**까지 끝까지 책임 (세로 분할)
- 공유 코드(공통 레이아웃, 공통 엔티티, 시큐리티 설정)는 Phase 0에서 함께 확정 후 이후엔 소유자만 수정

### 개발자 A — "계정·과정·학습" 축
| 도메인 | 주요 내용 |
|---|---|
| 인증/회원 | 로그인, 가입(강사/훈련생), 본인인증, 개인정보 동의, Spring Security, 세션/권한(관리자·강사·훈련생) |
| 사용자 관리 | 관리자의 강사/훈련생 등록·조회·수정, 분반, 접속 이력 |
| 과정/과목 | 과정 개설, 과목·차시 구성, 일정(스케줄러), 강사-과정 매핑, 수강신청(enroll) |
| 콘텐츠 | 콘텐츠 등록/라이브러리, VOD·문서 업로드, 진도 추적 저장(현재 JS 프론트 로직 → 서버 API) |
| 출결/이수 | 접속 기반 출결, 출결 현황, 이수 기준 설정, 자동 이수 처리, 이수증 PDF 발급 |

### 개발자 B — "평가·소통·데이터" 축
| 도메인 | 주요 내용 |
|---|---|
| 시험 | 문제은행, 시험 생성/설정(제한시간·재응시·부정행위 방지), 응시(입장 시 본인인증), 답안 저장/제출 |
| 과제 | 과제 등록, 제출(파일/링크/텍스트), 피드백 |
| 채점/성적 | 자동 채점, 수동(주관식) 채점, 성적 확정, 엑셀 다운로드 |
| 시험 모니터링 | 입·퇴장 로그, 녹화 목록, 이상행위 경고 |
| 소통 | Q&A, 튜터링, 공지사항, 알림, 설문조사(생성·배포·결과) |
| 대시보드/분석 | 관리자·강사·훈련생 대시보드 집계 쿼리, 학습 리포트, (후순위) EPS 이탈 예측·AI 챗봇 |

**분담 근거:** 두 축은 DB 테이블 의존이 한 방향(평가·소통은 사용자·과정을 *참조만* 함)이라 A가 먼저 만든 `User`/`Course` 엔티티 위에 B가 얹으면 되고, 서로 수정하는 파일이 겹치지 않는다. 업무량도 페이지 수 기준 대략 절반씩이다. 누가 A/B를 맡을지는 두 분이 정하면 되고, 프론트를 만든 분이 화면 수정이 많은 쪽(콘텐츠 플레이어·진도 연동이 있는 A, 또는 시험 응시 UX가 복잡한 B) 중 손에 익은 쪽을 잡는 것을 추천.

---

## 4. 단계별 일정

### Phase 0 — 공동 작업 (첫 주, 반드시 둘이 함께 확정)
1. **Spring Boot 프로젝트 생성**: Java 17+, Spring Boot 3.x, Spring Security, Spring Data JPA, Thymeleaf, MySQL(또는 MariaDB), Gradle
2. **ERD/스키마 확정**: IA 엑셀 "데이터 정리" 시트 기반. 핵심 테이블 — user(role), course, subject, session(차시), content, enrollment, attendance, exam, question, exam_attempt, answer, assignment, submission, grade, completion, certificate, notice, qna, survey, chat_log, access_log
   - 내역서 요건 반영: 과정 단위 데이터 독립 수집, soft delete(3년 보존), 개인정보 컬럼 암호화
3. **공통 레이아웃 fragment화**: 155개 페이지에 복붙된 사이드바/헤더/푸터를 Thymeleaf fragment로 추출 (이걸 안 하면 이후 모든 수정이 155번 반복됨)
4. **중복/오타 정리**: `index copy.html`, `form.html`, `alram/arlam→alarm`, `sesstion→session`, admin/instructor/trainee에 3중 복제된 `play-*`·`contents-*`·`grading-modal-*` 공통화
5. **협업 규칙**: `CLAUDE.md` 작성(아래 §5), Git 브랜치 전략(`main` 보호, `feat/a-*`, `feat/b-*`), 더미 JS 배열 → `data.sql` 시드 데이터 변환

### Phase 1 — 핵심 골격 (2~3주)
- **A**: Spring Security 로그인/가입/권한 3종 → 사용자 관리 CRUD → 과정/과목/차시 CRUD + 수강신청
- **B**: (A의 User/Course 엔티티 스텁 위에서) 문제은행 → 시험 생성/응시/제출 → 과제 등록/제출
- 마일스톤: **훈련생이 가입→수강신청→시험 응시까지 한 사이클이 돈다**

### Phase 2 — 운영 기능 (2~3주)
- **A**: 콘텐츠 업로드/플레이어 연동, 진도 추적 API, 출결 자동 기록, 이수 처리 + 이수증 PDF
- **B**: 자동/수동 채점, 성적 관리, 시험 모니터링(입퇴장 로그), Q&A/공지/알림/설문
- 마일스톤: **수료 판정과 채점이 실제 데이터로 동작한다**

### Phase 3 — 대시보드·증빙·마무리 (1~2주)
- **B**: 3개 역할 대시보드 집계, 학습 리포트, (여유 시) AI 챗봇·EPS
- **A**: HTTPS/SSL 적용, DB HA(복제) 구성, 백업 스크립트·백업 일지 (개인정보 컬럼 암호화와 접속 로그는 Phase 0~1에서 이미 구현된 것을 검증·스크린샷)
- **공동**: 내역서 증빙 작성 — §2-1 체크리스트 전 항목: 기능별 스크린샷, 서버 사양표·네트워크 구성도, 백업 정책, 보안 확인서, DB 암호화 스크린샷, DNS, LMS URL 표(훈련생/교강사/관리자 모듈)

---

## 5. Claude Code 2인 협업 규칙 (권장)

1. **CLAUDE.md를 저장소 루트에 공유** — 기술 스택, 패키지 구조(`com.ssa.lms.web.admin.*` 등 엑셀 확정안), 코딩 컨벤션, "A 소유 / B 소유" 디렉터리 목록을 적어두면 두 사람의 Claude가 같은 규칙으로 작업한다.
2. **도메인 = 디렉터리 = 소유자** — 자기 소유 패키지/템플릿 폴더만 수정. 공통 코드(fragment, 공통 엔티티, SecurityConfig) 수정은 상대에게 먼저 말한 뒤 진행.
3. **브랜치에서 작업 후 PR 머지** — `main` 직접 푸시 금지. 작은 단위로 자주 머지해서 충돌을 줄인다.
4. **엔티티 변경은 합의 후** — DB 스키마는 두 사람 모두에게 영향을 주므로, 변경 시 짧게라도 공유.
5. **Claude Code 플랜 모드 활용** — 큰 기능은 플랜을 먼저 뽑아 서로 겹치는 부분이 없는지 확인하고 구현 시작.
